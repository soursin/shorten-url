import { Request, Response } from "express";


export const alias = (req : Request , res : Response) => {

}

export const topic = (req : Request , res : Response) => {

}

export const overall = (req : Request , res : Response) => {
    
}